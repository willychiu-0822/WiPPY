---
name: wippy-start-work
description: Start WiPPY development work by clarifying the task, syncing main, and creating a correctly named branch. Use when the user says to start a new WiPPY task, start developing a feature, create a branch, begin feature work, begin a fix, or asks in Chinese such as "開始開發功能", "開始一個功能", "建立分支", "開一個工作分支", or "拉最新程式碼再開分支".
---

# WiPPY Start Work

Follow the shared command at `docs/agent-commands/wippy-start-work.md`.

This skill is only the Codex entrypoint. Do not duplicate operational steps here; the shared command is the source of truth for both Codex and Claude Code.
