---
name: wippy-start-work
description: Start WiPPY development work by syncing main and creating a correctly named branch. Use when the user says to start a new WiPPY task, create a branch, begin feature work, begin a fix, or asks in Chinese such as "開始一個功能", "建立分支", or "開一個工作分支".
---

# WiPPY Start Work

Follow the shared command at `docs/agent-commands/wippy-start-work.md`.

This skill is only the Codex entrypoint. Do not duplicate operational steps here; the shared command is the source of truth for both Codex and Claude Code.
