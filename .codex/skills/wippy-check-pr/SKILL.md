---
name: wippy-check-pr
description: Check whether a WiPPY pull request is ready to merge by reading GitHub PR status and CI checks. Use when the user asks "PR 有沒有過", "檢查 PR", "CI 有沒有過", "這個 PR 可以 merge 嗎", or wants PR readiness.
---

# WiPPY Check PR

Follow the shared command at `docs/agent-commands/wippy-check-pr.md`.

This skill is only the Codex entrypoint. Do not duplicate operational steps here; the shared command is the source of truth for both Codex and Claude Code.
