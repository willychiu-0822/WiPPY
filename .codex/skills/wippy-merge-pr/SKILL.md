---
name: wippy-merge-pr
description: Merge an approved WiPPY PR with squash merge after CI passes. Use only when the user explicitly asks to merge a PR, such as "merge 這個 PR", "合併 PR", "把 PR 合到 main", or "批准上線".
---

# WiPPY Merge PR

Follow the shared command at `docs/agent-commands/wippy-merge-pr.md`.

This skill is only the Codex entrypoint. Do not duplicate operational steps here; the shared command is the source of truth for both Codex and Claude Code.
